# shared

Code and utilities intended to be reused across more than one project in
this repository (common libraries, helper scripts, shared build
configuration, etc.). Empty for now — `sunflower-esp32-s3` is currently the
only project, so nothing has been factored out yet.
